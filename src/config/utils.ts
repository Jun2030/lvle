// Storage默认AES加密密钥
export const CRYPTO_SECRET = '__LeCryptoSecret__'

// Storage默认缓存时间（单位：分，默认：0分钟，永久缓存）
export const DEFAULT_CACHE_TIME = 0

// Storage是否加密,默认不加密
export const IS_ENCRYPT = false

// Storage-Forage默认配置
export const DEFAULT_FORAGE_CONFIG = {
  name: 'Le',
  version: 1.0,
  size: 4980736,
  storeName: 'Le',
}
